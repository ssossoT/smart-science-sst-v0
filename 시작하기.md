# 시작하기 (짧은 버전)

> **localhost 는 신경 쓰지 않으셔도 됩니다.**
> 내 컴퓨터에서 미리보는 기능일 뿐이고, 서버를 켜 둔 동안에만 열립니다.
> 건너뛰고 바로 배포하셔도 아무 문제 없습니다.

아래 **8단계**만 하시면 끝납니다.

---

## 1. Firebase 프로젝트 만들기

<https://console.firebase.google.com> → **프로젝트 추가**

만든 뒤 왼쪽 메뉴에서 세 가지를 켭니다.

| 메뉴 | 할 일 |
|---|---|
| **Authentication** | 시작하기 → Sign-in method → **Google** 사용 설정 → **이메일/비밀번호** 사용 설정 |
| **Firestore Database** | 데이터베이스 만들기 → 위치 `asia-northeast3` → **프로덕션 모드** |
| **Storage** | 시작하기 → 위치 같게 → **프로덕션 모드** |

---

## 2. 웹 앱 등록하고 설정값 복사

프로젝트 개요 화면에서 **`</>` (웹) 아이콘** 클릭 → 이름 아무거나 입력 → 등록

그러면 이런 내용이 나옵니다. **이걸 복사하세요.**

```js
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "...",
  projectId: "...",
  storageBucket: "...",
  messagingSenderId: "...",
  appId: "..."
};
```

> 나중에 다시 보려면: 톱니바퀴 → **프로젝트 설정** → 일반 → 아래로 스크롤 → **구성**

---

## 3. `js/firebase-config.js` 에 붙여넣기

내 컴퓨터에서 `js/firebase-config.js` 파일을 메모장이나 편집기로 열고,
`여기에-붙여넣기` 라고 적힌 부분을 2단계에서 복사한 값으로 바꿉니다.

```js
export const firebaseConfig = {
  apiKey: "AIzaSy...",          ← 이렇게 실제 값으로
  authDomain: "...",
  projectId: "...",
  storageBucket: "...",
  messagingSenderId: "...",
  appId: "..."
};
```

> 이 값은 공개되어도 괜찮은 값입니다. GitHub에 올라가도 문제없습니다.

---

## 4. 보안 규칙 붙여넣기 ⚠️ 꼭 하세요

이걸 안 하면 **로그인해도 아무 데이터가 안 보입니다.**

**① Firestore 규칙**
Firebase Console → **Firestore Database → 규칙** 탭
→ 기존 내용 전부 지우고 → `firestore.rules` 파일 내용 전체 붙여넣기 → **게시**

**② Storage 규칙**
Firebase Console → **Storage → 규칙** 탭
→ 기존 내용 전부 지우고 → `storage.rules` 파일 내용 전체 붙여넣기 → **게시**

---

## 5. GitHub에 올리기

폴더 전체를 GitHub 저장소에 올립니다. (Private 권장)

---

## 6. Vercel 배포

<https://vercel.com> → GitHub 계정으로 로그인 → **Add New → Project** → 저장소 Import

- Framework Preset: **Other**
- Build Command, Output Directory: **비워 둠**
- Environment Variables: 아래 5개 입력 (이미 아시는 방법대로)

```
GEMINI_API_KEY
YOUTUBE_API_KEY
FIREBASE_ADMIN_PROJECT_ID
FIREBASE_ADMIN_CLIENT_EMAIL
FIREBASE_ADMIN_PRIVATE_KEY
```

> 뒤의 3개는 Firebase Console → **프로젝트 설정 → 서비스 계정 → 새 비공개 키 생성**
> 으로 받은 JSON 파일 안의 `project_id` / `client_email` / `private_key` 값입니다.

**Deploy** 를 누르면 `https://___.vercel.app` 주소가 생깁니다.

---

## 7. 승인된 도메인 추가 ⚠️ 꼭 하세요

이걸 안 하면 **Google 로그인이 실패합니다.**

Firebase Console → **Authentication → 설정 → 승인된 도메인 → 도메인 추가**
→ 6단계에서 받은 주소 입력 (예: `smartlab.vercel.app`, `https://` 는 빼고)

---

## 8. 내 계정을 관리자로 등록

1. Vercel 주소 뒤에 `/teacher` 를 붙여서 접속
   예) `https://smartlab.vercel.app/teacher`
2. Google 계정으로 로그인
3. "관리자 권한이 없습니다" 화면이 뜨고 **UID** 가 보입니다 → **[UID 복사]**
4. Firebase Console → **Firestore Database → 데이터** 탭
5. **컬렉션 시작** → 컬렉션 ID에 `admins` 입력
6. **문서 ID** 칸에 복사한 UID 붙여넣기
7. 필드 하나 추가: 필드 이름 `name`, 유형 `string`, 값 `성소연` → **저장**
8. 브라우저 **새로고침** → 들어가집니다 ✅

---

## 다 됐습니다

이제 사이트 안에서 하시면 됩니다.

1. **설정** → 학교명·교사명·예산 확인 → **[2026 초기 일정 등록]** 누르기
2. **학생 관리** → 학생 추가 (비밀번호가 한 번만 보이니 바로 적어두세요)
3. **주차별 활동** → 수업 준비 시작

---

## 문제가 생기면

| 증상 | 원인 |
|---|---|
| 글씨만 나오고 디자인이 없음 | 파일을 더블클릭해서 열었을 때 생깁니다. Vercel 주소로 접속하세요 |
| Google 로그인 창이 바로 닫힘 | 7단계(승인된 도메인)를 안 했습니다 |
| 로그인은 되는데 아무것도 안 보임 | 4단계(보안 규칙)를 안 했습니다 |
| "관리자 권한이 없습니다" 반복 | 컬렉션 이름이 `admins` 인지, 문서 ID가 UID와 정확히 같은지 확인 |
| 학생 추가가 안 됨 | Vercel 환경변수 `FIREBASE_ADMIN_*` 3개 확인 후 Redeploy |

더 자세한 내용은 `README.md` 에 있습니다.
